const add = (a, b) => {
  return a + b;
};

const subs = (a, b) => {
  return a - b;
};

const div = (a, b) => {
  return a / b;
};

const mult = (a, b) => {
  return a * b;
};

const PI = 3.124;

// module.exports.add = add;
// module.exports.mult = mult;

module.exports = {
  add,
  mult,
  subs,
  div,
  PI,
};
