//Tuple is a varibale which can be declared in multiple type but it is fixed
//fixed length array with specific type

let tup: [string, number] = ["Tanvir", 28];
console.log(tup[0]);

//print with loop

tup.forEach((val) => {
  console.log(val);
});
